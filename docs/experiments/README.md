# Experiments

Results live here, not in memory. One row per experiment; the result file is the record.

| experiment | date | command | result file | verdict |
|---|---|---|---|---|
| golden set, rules judge 0.1.0 | 2026-09-07 | `make golden` then `scripts/golden.py --approve` | `golden.jsonl` (one line per run) | pass: 5/5 cases, both ordering constraints hold; rules 0.1.0 approved in `golden/approved.json` |
| learned judge v2, v3, v4 vs the golden set | 2026-09-08 | `scripts/train_judge.py v3 100`; `JUDGE_VERSION=v3 make judge`; `scripts/golden.py --judge-url http://localhost:8100` | `judge-v2.md`, `golden.jsonl` | all three refused: within tolerance on real boards, underestimate the saturated far-capacitor case (-10.3 / -14.4 / -14.1 vs -16.1); no learned judge approved |
| worker gate, both directions | 2026-09-08 | worker with `JUDGE_URL=http://localhost:8100 JUDGE_VERSION=v4`, then `=rules` | `judge-v2.md` (runs 58, 59) | v4 refused at the judge stage; remote rules judge accepted with the in-process score |
| Claude placer, Fable 5.1 | 2026-09-08 | `scripts/eval.py http://localhost:8000 3 --placer claude --fixtures rpi_hat` and the same for pic_programmer | `claude-placer.md`, `claude-placer/*.json` | rpi_hat: all 3 seeds route and verify, judge score identical to search, proxy cost 1-2% worse, 38-84 s per seed. pic_programmer: no proposal within the 20 min cap at high or medium effort; at low effort a 3-minute proposal with 7x the proxy cost, 16 unrouted, failed verification. Search wins on the big board; a draw on the small one |
| field-solver oracle validated | 2026-09-08 | `pytest tests/test_fields.py -s` | `fields-oracle.md` | z0 within 0.6% and eps_eff within 2% of Hammerstad-Jensen for w/h 0.5-3; 0.36 s per solve; pair coupling falls monotonically with spacing |
| golden set recaptured from the oracle; impedance-live cases added | 2026-09-08 | `scripts/golden.py --capture`, `--physics oracle --update`, then `make golden` | `golden.jsonl`, `../../golden/README.md` | seven cases; rpi_fast scores -20.58 (oracle) vs -20.88 (closed form); the closed form passes against the oracle's answers |
| dataset 17: 5,000 cross-sections solved by the oracle | 2026-09-08 | `make dataset SHARDS=4 N=1250 SEED=1`; `scripts/dataset.py wait 17` | S3 `datasets/17/manifest.json`, `../DATA.md` | 4 shards x 1250 in 109-114 s each on 16 cores; 2475 single, 2525 pairs |
| surrogate v5 trained and gated | 2026-09-08 | `make train-surrogate DATASET=17 VERSION=v5`; `JUDGE_VERSION=v5 make judge`; `scripts/golden.py --judge-url http://localhost:8100 --approve` | `surrogate-v5.md`, `golden.jsonl` | held-out MAPE 0.9% (z0), 1.9%/1.7% (odd/even); fresh-oracle MAPE 0.9% (p95 2.4%); passes all 7 golden cases against the oracle (rpi_fast -20.25 vs -20.58); approved |
| worker judging with surrogate v5 | 2026-09-08 | worker with `JUDGE_URL=http://localhost:8100 JUDGE_VERSION=v5`; generate the HAT with I2C nets as high-speed | run 76 | judge stage `learned-fd v5`; 0.2 mm I2C traces predicted at 134 ohm, flagged against 50; run verified |
| general cut solver validated | 2026-09-09 | `pytest tests/test_fields.py -k cut` | `../../tests/test_fields.py` | `fields.solve_cut` reduces to `solve` within 0.5% (one trace) and 1% (pair modes), matches the thickness-corrected coplanar-waveguide closed form within 3% with no plane, matrix symmetric, charge = energy, far neighbour < 1%, coupling monotone, grid converged; ~0.2-1.6 s per cut |
| golden answer key recaptured with real cross-sections | 2026-09-09 | `scripts/golden.py --physics oracle --update` | `../../golden/README.md`, `golden.jsonl` | five plane-less cases unchanged; `rpi_fast` -20.58 -> -24.18 and `rpi_fast_tuned` -3.43 -> -4.16 because the HAT has no plane under its I2C traces and the old key assumed one |
| dataset 45: 8,000 synthetic cuts solved by the general solver; 41 boards windowed and relabelled | 2026-09-09 | `make dataset KIND=cut SHARDS=8 N=1000 SEED=1`; `scripts/factory.py add-runs`; `make factory-windows` | S3 `datasets/45/manifest.json`, `windows` rows | 8 shards x 1,000 in 204-451 s each; 508 windows, 5,206 cuts, 4,063 with a reference, 46 with a plane |
| cut model v7/v8 vs gradient boosting | 2026-09-09 | `make train-cutnet DATASET=45 VERSION=v8 REAL=pic_programmer` and the runs in `cutnet.md` | `cutnet.md`, `cutnet-v7.md`, `cutnet-v8.md` | v8: 0.6% on held-out synthetic, 1.8% (p95 4.5%) on the never-seen rpi_hat family vs 3.5% for the baseline; synthetic-only v7 does not transfer (16%): the sampler is off the real-board distribution |
| rules 0.1.0 and learned-fd v5 against the recaptured key | 2026-09-09 | `make golden`; `JUDGE_VERSION=v5 make judge` + `make golden JUDGE_URL=...` | `golden.jsonl` | both FAIL `rpi_fast` (-20.88 / -20.25 vs -24.18) and `rpi_fast_tuned` (-3.43 vs -4.16, missing the `/ID_SCL` impedance violation): they assume a plane the HAT does not have |
| learned-cut v8 gated and approved | 2026-09-09 | `JUDGE_VERSION=v8 make judge`; `scripts/golden.py --judge-url http://localhost:8100 --approve` | `golden.jsonl`, `judge_approvals` row | PASS 7/7 (`rpi_fast` -23.98 vs -24.18, `rpi_fast_tuned` -4.15 vs -4.16); approved against golden b2033532db5b |
| worker judging with learned-cut v8 | 2026-09-09 | worker with `JUDGE_URL=http://localhost:8100 JUDGE_VERSION=v8`; `POST /designs/76/runs?mode=generate` | run 86 | judge stage `learned-cut v8`; `/ID_SCL` 122 ohm and `/ID_SDA` 153 ohm from 15 and 11 real cross-sections (`reference: cut`), both flagged against 50; run verified |

